{{- if .Values.service.nexus.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus.fullname" . }}
{{- if .Values.service.annotations }}
  annotations:
    {{ toYaml .Values.service.annotations | nindent 4 }}
{{- end }}
  labels:
  {{- include "nexus.labels" . | nindent 4 }}
  {{- if .Values.nexus.extraLabels }}
    {{- with .Values.nexus.extraLabels }}
      {{ toYaml . | nindent 4 }}
    {{- end }}
  {{- end }}
spec:
  type: {{ .Values.service.nexus.type }}
  selector:
    {{- include "nexus.selectorLabels" . | nindent 4 }}
    {{- if .Values.nexus.extraSelectorLabels }}
      {{- with .Values.nexus.extraSelectorLabels }}
        {{ toYaml . | nindent 4 }}
      {{- end }}
    {{- end }}
  ports:
    - name: nexus-ui
      protocol: {{ .Values.service.nexus.protocol }}
      port: {{ .Values.service.nexus.port }}
      targetPort: {{ .Values.service.nexus.targetPort }}
{{- end }}
---
{{- if .Values.bootstrap.repositories.dockerProxy -}}
{{ range $registry := .Values.bootstrap.repositories.dockerProxy }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus.fullname" $ | trunc 49 }}-docker-{{ $registry.port }}
  {{- if $.Values.service.annotations }}
  annotations:
    {{ toYaml $.Values.service.annotations | nindent 4 }}
  {{- end }}
  labels:
    {{- include "nexus.labels" $ | nindent 4 }}
    {{- if $.Values.nexus.extraLabels }}
      {{- with $.Values.nexus.extraLabels }}
        {{ toYaml . | nindent 4 }}
      {{- end }}
    {{- end }}
spec:
  type: ClusterIP
  selector:
    {{- include "nexus.selectorLabels" $ | nindent 4 }}
    {{- if $.Values.nexus.extraSelectorLabels }}
      {{- with $.Values.nexus.extraSelectorLabels }}
        {{ toYaml . | nindent 4 }}
      {{- end }}
    {{- end }}
  ports:
    - name: docker-{{ $registry.port }}
      protocol: TCP
      port: {{ $registry.port }}
      targetPort: {{ $registry.targetPort }}
---
{{ end }}
{{- end }}
{{- if .Values.bootstrap.repositories.dockerHosted -}}
{{ range $registry := .Values.bootstrap.repositories.dockerHosted }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus.fullname" $ | trunc 49 }}-docker-{{ $registry.port }}
  {{- if $.Values.service.annotations }}
  annotations:
    {{ toYaml $.Values.service.annotations | nindent 4 }}
  {{- end }}
  labels:
    {{- include "nexus.labels" $ | nindent 4 }}
    {{- if $.Values.nexus.extraLabels }}
      {{- with $.Values.nexus.extraLabels }}
        {{ toYaml . | nindent 4 }}
      {{- end }}
    {{- end }}
spec:
  type: ClusterIP
  selector:
    {{- include "nexus.selectorLabels" $ | nindent 4 }}
    {{- if $.Values.nexus.extraSelectorLabels }}
      {{- with $.Values.nexus.extraSelectorLabels }}
        {{ toYaml . | nindent 4 }}
      {{- end }}
    {{- end }}
  ports:
    - name: docker-{{ $registry.port }}
      protocol: TCP
      port: {{ $registry.port }}
      targetPort: {{ $registry.targetPort }}
---
{{ end }}
{{- end }}
{{- if .Values.bootstrap.repositories.dockerGroup -}}
{{ range $registry := .Values.bootstrap.repositories.dockerGroup }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus.fullname" $ | trunc 49 }}-docker-{{ $registry.port }}
  {{- if $.Values.service.annotations }}
  annotations:
    {{ toYaml $.Values.service.annotations | nindent 4 }}
  {{- end }}
  labels:
    {{- include "nexus.labels" $ | nindent 4 }}
    {{- if $.Values.nexus.extraLabels }}
      {{- with $.Values.nexus.extraLabels }}
        {{ toYaml . | nindent 4 }}
      {{- end }}
    {{- end }}
spec:
  type: ClusterIP
  selector:
    {{- include "nexus.selectorLabels" $ | nindent 4 }}
    {{- if $.Values.nexus.extraSelectorLabels }}
      {{- with $.Values.nexus.extraSelectorLabels }}
        {{ toYaml . | nindent 4 }}
      {{- end }}
    {{- end }}
  ports:
    - name: docker-{{ $registry.port }}
      protocol: TCP
      port: {{ $registry.port }}
      targetPort: {{ $registry.targetPort }}
---
{{ end }}
{{- end }}
